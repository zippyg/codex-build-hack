# architecture

No application architecture exists yet. The current repository layout is an agent operating system plus a versioned organiser-guide source record in `docs/source/` and an execution brief in `docs/specs/`.
