/goal

Objective
Take PROMPTECTOMY from the accepted Phase 0 design to a security-reviewed, performance-qualified, publicly released stable v1. Continue through every phase without stopping at milestones; complete only when the full definition of done has evidence.

Read first
AGENTS.md, .agent/state/STATUS_2026-07-18.md, and .agent/state/FORWARD_PLAN_2026-07-19.md in full, then its required read order. Re-verify Git/source truth at every phase.

Scope
Build every master-plan phase: safe Python oracle; OCI executor; hostile clean install; Contract v2/SQLite/CAS/API/reports; Rust core/CLI; safe Git/archive; Python+TS Responses adapters/evidence; isolated Codex evaluation; TUI; Tauri GUI with Claude design pass; agent office; safe Apply; cross-platform hardening; governance, supply chain, approved public release and anonymous verification. Hosted multi-tenant execution and automatic deployment are separate products.

Success
- Inspect/Audit/Draft never mutate targets; untrusted code never runs in core/clients; no credential/privacy canary leaks.
- Unsupported/zero-work/failure/cancel are typed; no weaker fallback; Apply is explicit, stale-checked, recoverable, non-default-branch only.
- Stable Python+TS matrix, evaluation, receipts, client/report parity, hostile corpus, SLOs, clean installs and public quickstart all pass.
- Every release claim maps to tests/reviews; no unresolved P0/P1 or critical/high issue.

Plan
0T safely commit planning on zc/product-v1, excluding protected user diff. 1A safe Python modes/unverified patches. 1B accepted executor. 1C hostile installed artifacts. 2 contracts/state/API/reports. 3 Rust core/CLI. 4 Git/adapters/capture/evidence/privacy. 5 isolated Codex evaluation. 6 TUI. 7 Tauri GUI/design/accessibility. 8 agents/Apply/integrations. 9 full RC/supply-chain hardening. 10 obtain explicit human release decisions, publish, anonymously verify, and close.

Persistence
Do not mark complete at phase exits. Update the phase ledger/status, test, review, resolve P0/P1, commit/push safe work, and continue. On a genuine human-only blocker, log it, ping once with exact input needed, build all independent work, and keep the goal active. Never silently stop or weaken a gate.

Constraints
Full detail in .agent/state/FORWARD_PLAN_2026-07-19.md - read it first. Preserve engine/promptectomy/generated/route_ticket.py at SHA-256 a4fcdab0f1baef70072620e1409d68e732c5a4549d3f36388a7213237dab961c; never stage/restore/overwrite/commit/decide it without Zain. Respect locks, use synthetic fixtures and primary sources, bound agents/execution, run phase acceptance/security reviews, and never use destructive Git or leak secrets. Publicity, license, name, signing, deploy and real-repo Apply require explicit approval.

Done = every checklist item in the master plan is evidenced, the approved public release works from clean anonymous installs, the final tree is clean with user-approved protected-diff disposition, and no required work/P0/P1 remains.
