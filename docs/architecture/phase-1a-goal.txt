/goal

Objective
Implement PROMPTECTOMY Phase 1A only: make the existing Python CLI an honest reference for non-mutating Inspect, Audit, and Draft-unverified, with durable safe artifacts and typed terminal outcomes. Do not start isolation, evaluation, Apply, Rust, TUI, or GUI work.

Read first
Read AGENTS.md, .agent/state/STATUS_2026-07-18.md, and docs/architecture/phase-1a-handoff.md in full, then follow the handoff's required read order. Verify current Git/source truth before editing.

Immutable boundary
- Preserve engine/promptectomy/generated/route_ticket.py exactly. Phase 0 entry SHA-256: a4fcdab0f1baef70072620e1409d68e732c5a4549d3f36388a7213237dab961c.
- Never stage, restore, overwrite, format, regenerate, commit, or decide that file's disposition.
- Preserve all unrelated user work. Use apply_patch for edits. No destructive Git.

Build
1. Add explicit doctor, inspect, audit, draft, status, and JSON/Markdown report semantics to the existing Python/Typer reference surface.
2. Inspect/Audit/Draft must never change target files, index, refs, config, hooks, worktrees, nested repos, or untracked inventory. Hash and test these boundaries.
3. Inspect inventories support and exclusions. Audit performs supported static analysis only. Neither imports repository modules, runs target commands, installs dependencies, invokes hooks/filters, or executes generated code.
4. Draft requires explicit authority and private tool-owned storage outside the target checkout/.git. Implement a trusted direct OpenAI Responses connector to an API-available Codex coding model with strict structured output, no tool definitions, and only egress-manifest-approved source slices. Do not launch codex exec, Codex SDK threads, MCP, shell, filesystem, web, hosted-shell, apply-patch, or computer-use tools on the general path. Record safe model/schema/prompt/content/usage metadata. If this boundary, model capability, egress, or budget is unavailable, return typed safe_agent_transport_unavailable and create no patch. Valid output becomes an unverified patch plus proposed tests. Never apply/execute it or fall back to legacy synthesis/replay.
5. Persist safe append-only events, typed errors, terminal status, support/unsupported accounting, content-addressed artifacts, digest-bound receipt wording, retention/cleanup state, and deterministic machine output. No secrets, raw environment values, credentials, unsafe exceptions, protected payloads, or source bodies in safe events.
6. Quarantine or clearly gate the hackathon demo-only general run path so it cannot claim arbitrary-repository success. Zero performed work must be completed_no_findings, completed_with_unsupported, or failure based on evidence, never generic success.

Acceptance
Use synthetic repositories only: supported Python, TS inspect coverage, mixed unsupported, no callsites, malformed/denied, dirty Git, symlink/traversal/nested repo, prompt injection, privacy canaries, Codex invalid output/timeout/budget/cancel. Exercise relevant INV-AUTH, GIT, CONNECTOR, AGENT, STATE, CONTRACT, RECEIPT, CLI, REPORT, and PRIV gates from docs/architecture/testing-and-benchmarks.md. Use an injected client/HTTP double to prove the exact no-tool request. A live synthetic model probe is optional and requires explicit approval/egress consent. Build/install a clean wheel in a disposable environment and prove its Phase 1A assets load. Run focused tests and fresh architecture/security/diff reviews; resolve every P0/P1.

Out of scope
No OCI/WASI/microVM executor; verified Draft/evaluation/holdout verdict; hostile Phase 1C; Apply/Integrate/hot-swap; real/private repo or customer trace; private Git broker/submodule/LFS/filter/archive work; Rust/daemon/SQLite/local API/TUI/Tauri/GUI; deploy/publish/public release; commit/push unless explicitly requested.

Done
The three modes are demonstrably non-mutating; Draft artifacts are explicitly unverified and never executed; unsupported work and failure are typed; installed-wheel/privacy/agent-isolation tests pass; product/docs match; route_ticket.py is unchanged; all P0/P1 review findings are resolved. Update the authoritative status and write a Phase 1A evidence handoff. Do not proceed into Phase 1B.
